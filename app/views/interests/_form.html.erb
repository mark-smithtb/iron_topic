<%= form_for([@topic, @interest], class: "form-horizontal") do |f| %>
  <% if @interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interest.errors.count, "error") %> prohibited this interest from being saved:</h2>

      <ul>
      <% @interest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :score %><br>
    <%= f.radio_button(:score, 1)%>
    <%= f.radio_button(:score, 2)%>
    <%= f.radio_button(:score, 3)%>
    <%= f.radio_button(:score, 4)%>
    <%= f.radio_button(:score, 5)%>
  </div>
  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
